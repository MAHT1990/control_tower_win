<UserControl x:Class="ControlTowerWin.Features.EmbeddedTerminal.Views.TerminalSessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ControlTowerWin.Features.EmbeddedTerminal.ViewModels"
             xmlns:local="clr-namespace:ControlTowerWin.Features.EmbeddedTerminal.Views"
             xmlns:ctrl="clr-namespace:ControlTowerWin.Shared.Controls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- 인라인 rename: 편집 중이면 라벨 숨김/TextBox 노출. 기본값은 로컬 속성이 아니라
             스타일 Setter로 둔다(로컬 값이 트리거를 이겨 버리는 함정 회피 — runbook 07 참조). -->
        <Style x:Key="RenameLabel" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RenameBox" TargetType="TextBox">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="MinWidth" Value="80"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- 좌측 트리: 탭(부모) → 터미널(자식, PID). 라벨 더블클릭/F2/컨텍스트 메뉴로 이름 변경(FR-046) -->
        <HierarchicalDataTemplate DataType="{x:Type vm:TabViewModel}"
                                  ItemsSource="{Binding Terminals}">
            <Grid>
                <TextBlock Text="{Binding Title}" FontWeight="Bold"
                           Style="{StaticResource RenameLabel}"
                           MouseLeftButtonDown="Label_MouseDown"/>
                <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                         Style="{StaticResource RenameBox}"
                         IsVisibleChanged="RenameBox_VisibleChanged"
                         KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus"/>
            </Grid>
            <HierarchicalDataTemplate.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:TerminalViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <!-- 다중 주입 대상 체크(FR-014 다중 세션 독립 주입) -->
                        <CheckBox IsChecked="{Binding IsInjectTarget}" VerticalAlignment="Center"
                                  Margin="0,0,4,0" ToolTip="다중 주입 대상"/>
                        <Grid>
                            <TextBlock Text="{Binding TreeLabel}"
                                       Style="{StaticResource RenameLabel}"
                                       MouseLeftButtonDown="Label_MouseDown"/>
                            <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                     Style="{StaticResource RenameBox}"
                                     IsVisibleChanged="RenameBox_VisibleChanged"
                                     KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus"/>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </HierarchicalDataTemplate.ItemTemplate>
        </HierarchicalDataTemplate>

        <!-- 우측 pane: 라이브 터미널. 활성 pane만 파란 테두리(기본값 스타일 Setter + 트리거) -->
        <DataTemplate x:Key="TerminalPane" DataType="{x:Type vm:TerminalViewModel}">
            <Border Margin="2" BorderThickness="2" GotKeyboardFocus="Pane_GotFocus">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <local:TerminalView/>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 좌 트리 + 우 pane 분할 -->
        <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 좌측: 탭>터미널 트리 + 조작 + 우클릭 세션 액션 컨텍스트 메뉴(SC-23) -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,4">
                <Button Content="새 탭" Command="{Binding AddTabCommand}" Margin="0,0,4,0" Padding="6,2"/>
                <Button Content="새 터미널" Command="{Binding AddTerminalCommand}" Margin="0,0,4,0" Padding="6,2"/>
                <Button Content="닫기" Command="{Binding CloseCommand}" Padding="6,2"/>
            </StackPanel>
            <!-- 커맨드 주입 바(FR-014): 선택 터미널에 주입. "명령 실행" 컨텍스트 메뉴가 여기 포커스. -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,4,0,0">
                <TextBox x:Name="CommandBox" Width="140"
                         Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="CommandBox_KeyDown"/>
                <Button Content="실행" Command="{Binding InjectCommand}" Margin="4,0,0,0" Padding="6,2"/>
            </StackPanel>
            <TreeView x:Name="SessionTree" ItemsSource="{Binding Tabs}"
                      SelectedItemChanged="OnTreeSelectionChanged"
                      PreviewMouseRightButtonDown="OnTreeRightClick"
                      PreviewKeyDown="OnTreeKeyDown">
                <TreeView.ContextMenu>
                    <!-- SC-23: 우클릭 노드에 대한 세션 액션. 우클릭 시 코드비하인드가 노드를 먼저 선택. -->
                    <ContextMenu>
                        <MenuItem Header="명령 실행"
                                  Command="{Binding PlacementTarget.DataContext.RunCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="이름 변경"
                                  Command="{Binding PlacementTarget.DataContext.RenameCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <Separator/>
                        <MenuItem Header="재시작"
                                  Command="{Binding PlacementTarget.DataContext.RestartCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="종료"
                                  Command="{Binding PlacementTarget.DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    </ContextMenu>
                </TreeView.ContextMenu>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </DockPanel>

        <!-- 우측: 탭들을 한 셀에 겹쳐 살려두고(keep-alive) 선택된 탭만 표시. UniformGrid pane 분할. -->
        <ItemsControl Grid.Column="1" Margin="8,0,0,0" ItemsSource="{Binding Tabs}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Visibility"
                            Value="{Binding IsSelected, Converter={StaticResource BoolToVis}}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:TabViewModel}">
                    <ItemsControl ItemsSource="{Binding Terminals}"
                                  ItemTemplate="{StaticResource TerminalPane}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ctrl:ResizableTilePanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </Grid>

        <!-- Row 1: SC-24 출력 캡처 버퍼 (FR-047, mechanism c: A출력→편집→대상 주입) -->
        <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Margin="0,6,0,0" Padding="0,6,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="출력 캡처 버퍼" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CaptureBuffer.SourceAs, StringFormat=' (from {0})'}"
                               VerticalAlignment="Center" Foreground="Gray"/>
                    <Button Content="캡처" Command="{Binding CaptureCommand}" Margin="8,0,0,0" Padding="6,2"
                            ToolTip="선택 터미널의 현재 출력을 버퍼로 수집"/>
                    <Button Content="대상에 주입" Command="{Binding RouteCommand}" Margin="4,0,0,0" Padding="6,2"
                            ToolTip="버퍼 내용을 체크된 대상(없으면 선택 터미널)에 주입"/>
                </StackPanel>
                <TextBox Text="{Binding CaptureBuffer.Content, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                         Height="80" FontFamily="Consolas"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
